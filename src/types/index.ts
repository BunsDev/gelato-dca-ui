
export type DCAPosition = {
  positionId: string
  tokenIn: string
  tokenOut: string
  balanceIn: string
  balanceOut: string
  amountDCA: string
  intervalDCA: string
  lastDCA: string
  maxSlippage: string
}